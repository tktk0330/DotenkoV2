import SwiftUI

struct GameMainView: View {
    
    var body: some View {
        VStack(spacing: 24) {
            Text("が参加中")
                .font(.system(size: 16, weight: .medium))
                .foregroundColor(.white)
        }
    }
}
